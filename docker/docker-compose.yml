version: '3.9'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ranch
      POSTGRES_PASSWORD: ranch
      POSTGRES_DB: ranch
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
